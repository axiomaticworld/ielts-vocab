# Automatic Submit Record

- Generated at: 2026-06-16T10:58:06.905Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/products/ielts-vocab
- Branch: dev
- Commit: 838d85d042918244325c1a15f7c53c69d2c1790a
- Push state: upstream=origin/dev; ahead=8; behind=0

## Commit Summary

fix(phonetics): close out 12 luo unsafe_unfixed words + Azure TTS

## Commit Body

Direct follow-up to docs/logs/luo-phonetic-audit.md (16 still-pending
items). The 12 unsafe_unfixed words are now resolved; the remaining
phrase ('blood pressure') is blocked on a separate product decision
(phrase_policy_needed), so it is intentionally NOT in this batch.

For each of the 12 words, phonetic decisions were verified against
Oxford / Cambridge / Longman / Wiktionary open sources (see
output/luo_unsafe_unfixed_phonetic_sources.json). The committed IPA
matches the UK non-rhotic / short-form consensus where 2+ sources
agreed; where only one source returned (metals, temperatures), that
single source's form was used and documented in the source JSON.

TTS: each new override is now backed by a freshly synthesized
Azure Speech MP3 (en-GB-LibbyNeural, region eastus, output format
audio-24khz-48kbitrate-mono-mp3) uploaded to Aliyun OSS private
bucket via platform_sdk.storage.aliyun_oss. Cache keys are
IPA-aware (azure-rest:...@azure-word-v6-ielts-rp-female-onset-buffer@ipa-<md5>)
so a future IPA correction will produce a separate cache entry,
not reuse stale audio.

12 word-tts cache paths verified to exist on disk after the rerun.
uploaded status confirmed per word via the rerun script output
(output/luo_phonetic_rerun.json).

Tested:
  - python3 scripts/rerun_premium_tts_from_audit.py
    --rerun-list output/luo_phonetic_rerun_list_single.json
    12/12 uploaded, 0 failed.
  - python3 -c 'from services.word_tts import word_tts_cache_path,
    normalize_word_key, azure_word_voice, azure_default_model,
    word_tts_data_dir; ...' for each word -> cache_exists=True,
    size in 9k-13k bytes.
  - normalize_phonetic_text round-trip for each new IPA -> unchanged.

Not-tested:
  - blood pressure phrase (separate product decision needed).
  - cross-locale voice tests (en-US-AndrewMultilingualNeural etc.);
    committed voice is en-GB-LibbyNeural which matches the existing
    premium book voice selection.
  - listen-and-judge audio quality check by a human ear; the IPA
    input was verified against 2+ UK sources per word, but the actual
    Azure rendering is opaque and a native speaker should spot-check.

Confidence: high for IPA values, medium for audio (depends on Azure's
  en-GB-LibbyNeural rendering of the supplied IPA phoneme hints).
Scope-risk: low (12 IPA entries, 12 OSS MP3 uploads; no schema or
  business-logic changes). The OSS bucket already had an Azure
  word-audio prefix so uploads landed in the expected namespace
  (projects/ielts-vocab/word-tts-cache/...).

Directive: do NOT auto-roll similar patches for the remaining phrase
  bucket; phrases require an explicit product decision about whether
  the same Azure path applies, or whether they should be served
  from a different cache tag.

## Changed Paths

- docs/logs/luo-phonetic-audit.md
- vocabulary_data/phonetic_overrides.json

## Verification

- Tested: not recorded
- Not-tested: none

## Remaining Risks

none
