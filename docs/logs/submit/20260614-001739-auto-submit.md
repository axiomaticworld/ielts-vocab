# Automatic Submit Record

- Generated at: 2026-06-13T16:17:39.841Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/products/ielts-vocab
- Branch: dev
- Commit: 4c7188116f180ab1308c6dafffa9132b90929d63
- Push state: upstream=origin/dev; ahead=1; behind=0

## Commit Summary

ci(hooks): add axi-rules-hooks workflow for AR-GIT-005 server-side gate

## Commit Body

Add a server-side GitHub Actions workflow that re-installs the generated .githooks/commit-msg and runs workspace-git-hooks.mjs verify in self-only mode. The workflow is the server-side safety net for the local commit-msg hook: any push that bypasses the local hook (git push --no-verify, hand-rolled commit pipeline) is rejected here with a clear actionable error. The path filter limits the trigger to changes that actually affect the gate; the workflow uses AXI_HOOKS_SELF_ONLY=1 so the audit only reports the owning repository (each CI checkout contains one axiom-owned repository plus a sibling governance checkout). Spec: projects/axi-rules/docs/specs/2026-06-13-ci-rollout/PRD.md.

Constraint: The workflow file is byte-identical across the 15 axiom-owned repositories receiving it in this rollout; the workflow is small (under 100 lines of YAML) and self-documenting. Pull requests that do not touch .githooks/, governance scripts, or the workflow file itself are ignored.
Rejected: Using a server-side git hook | GitHub Actions on push and pull_request is the standard gate and matches the existing patterns. Rejected: Running verify without AXI_HOOKS_SELF_ONLY=1 | The full-workspace verify would fail in a single-repo CI checkout because the other 16 axiom-owned repositories are not on disk. Rejected: Adding a step to publish a workflow artifact | The workflow is deterministic; no artifact is needed.
Confidence: high
Scope-risk: narrow
Directive: After this commit lands, the axiom-owned repository has a server-side gate that mirrors the local commit-msg hook. Operators can use the workflow_dispatch trigger to re-verify the gate on demand.
Tested: pnpm --dir infra/axi-workspace-governance git-hooks:test -> 12/12 pass in the dev container; the new workflow YAML is syntactically valid; pnpm --dir infra/axi-workspace-governance workspace:git:hooks:audit still reports commit-msg=executable for this repository; pnpm --dir infra/axi-workspace-governance workspace:git:hooks:verify still exits 0 in the full workspace.
Not-tested: The CI workflow run itself (will be exercised on next push to this repository); future axiom-owned repositories added to workspace.graph.json after this rollout.

## Changed Paths

- .github/workflows/axi-rules-hooks.yml

## Verification

- Tested: pnpm --dir infra/axi-workspace-governance git-hooks:test -> 12/12 pass in the dev container; the new workflow YAML is syntactically valid; pnpm --dir infra/axi-workspace-governance workspace:git:hooks:audit still reports commit-msg=executable for this repository; pnpm --dir infra/axi-workspace-governance workspace:git:hooks:verify still exits 0 in the full workspace.
- Not-tested: The CI workflow run itself (will be exercised on next push to this repository); future axiom-owned repositories added to workspace.graph.json after this rollout.

## Remaining Risks

The CI workflow run itself (will be exercised on next push to this repository); future axiom-owned repositories added to workspace.graph.json after this rollout.
