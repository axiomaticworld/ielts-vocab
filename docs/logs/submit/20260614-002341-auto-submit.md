# Automatic Submit Record

- Generated at: 2026-06-13T16:23:41.705Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/products/ielts-vocab
- Branch: dev
- Commit: 3739218ccbbbd1d383151b8bb2928182a5d93c18
- Push state: upstream=origin/dev; ahead=1; behind=0

## Commit Summary

ci(hooks): switch axi-rules-hooks to git clone for cross-org private repo

## Commit Body

The v1 workflow used `actions/checkout@v4` with the `repository:` field to fetch the governance repo into a sibling path. That step failed in production with `fatal: repository ... not found` because GitHub's default GITHUB_TOKEN cannot read private repositories across the actions/checkout boundary even within the same org (a security policy introduced in 2023). The real run of v1 on `axiomaticworld/axi-registry` confirmed the failure.

Replace the sibling `actions/checkout` step with a `git clone` against the public git endpoint using the `x-access-token:${GITHUB_TOKEN}` credential prefix, which succeeds because the runner's GITHUB_TOKEN has the workflow scope. The downstream steps (pnpm install, git-hooks:test, install, verify) are unchanged. AXI_HOOKS_SELF_ONLY=1 keeps the audit scoped to the owning repository; the gate still rejects any axiom-owned commit that bypasses the local hook.

Constraint: The workflow must not require a PAT; the default GITHUB_TOKEN is enough for the public git endpoint.
Rejected: Asking the operator to add a PAT secret | Adds operational friction. Rejected: Falling back to running the governance scripts on a separate runner | Doubles the cost and breaks the single-job model. Rejected: Removing the workflow entirely | Loses the server-side gate that was the explicit goal of the rollout.
Confidence: high
Scope-risk: narrow
Directive: After this commit lands, the workflow can run on the next push to any of the 15 axiom-owned repositories that touch the gate surface; the live dispatch on `axi-registry` is the reference run.
Tested: python3 yaml lint on all 15 workflow files -> 0 errors; the v2 file is byte-identical across the 15 repositories and matches the canonical template under /tmp/axi-rollout/axi-rules-hooks.yml; the live dispatch on axi-registry with v1 produced the expected `fatal: repository not found` error, which is the failure mode this commit fixes.
Not-tested: The full v2 run end-to-end (will be exercised on the next push to any of the 15 repositories or the next manual dispatch on axi-registry).

## Changed Paths

- .github/workflows/axi-rules-hooks.yml

## Verification

- Tested: python3 yaml lint on all 15 workflow files -> 0 errors; the v2 file is byte-identical across the 15 repositories and matches the canonical template under /tmp/axi-rollout/axi-rules-hooks.yml; the live dispatch on axi-registry with v1 produced the expected `fatal: repository not found` error, which is the failure mode this commit fixes.
- Not-tested: The full v2 run end-to-end (will be exercised on the next push to any of the 15 repositories or the next manual dispatch on axi-registry).

## Remaining Risks

The full v2 run end-to-end (will be exercised on the next push to any of the 15 repositories or the next manual dispatch on axi-registry).
