# Automatic Submit Record

- Generated at: 2026-06-16T20:05:39.666Z
- Generated-by: axi-submit-log-post-commit
- Repository: /Volumes/code/workspace/products/ielts-vocab
- Branch: dev
- Commit: 7d48bfaa2b3612ed5bac2288bda3cfef746e7473
- Push state: upstream=origin/dev; ahead=14; behind=0

## Commit Summary

fix(phonetics): apply 130 catalog-wide unsafe IPA overrides + Azure TTS

## Commit Body

Continuation of the catalog-wide IPA audit (docs/logs/all-ipa-audit-final.md).
The 130 'unsafe_in_catalog' recommendations from that report are
now applied as phonetic_overrides.json entries, with fresh Azure
word-tts MP3s synthesized and uploaded to the production cache path.

Override strategy: prefer Cambridge UK (most authoritative for RP),
fall back to Wiktionary RP, then Wikt GA / Camb US / ipa-dict.
This avoids the corner case where Wiktionary's RP form includes
a non-standard long vowel (e.g. Wikt lists /æk.təː/ for 'actor'
but RP is /æk.tə/; Cambridge UK is the canonical source).

TTS: en-GB-LibbyNeural, azure-rest:audio-24khz-48kbitrate-mono-mp3,
SSML <phoneme alphabet='ipa' ph='...'> from the override value,
uploaded to axi-shared-private-sz OSS bucket at the production
cache path (no IPA digest suffix, matching production lookup).

Results:
  - 130 overrides written to vocabulary_data/phonetic_overrides.json
    (count: 580 -> 708)
  - 130/130 Azure TTS synthesized, all 130 local cache files written
  - 128/130 production metadata API responses match the new
    uploads (byte_length + etag)
  - 2/130 (consistency, manufacturer): production metadata API
    returns 404 for these words. Investigation:
      - Both words exist in the local catalog (multiple JSON files
        including ielts_confusable_match.json and ielts_9400_extended.json)
      - Local override is correctly written
      - Local word_tts_cache and OSS both have the new files
        (verified via direct head_object on OSS; both return
        200 with the expected size and etag)
    - Root cause is on the production side: the metadata endpoint
      does not find these two words in its catalog/cache lookup.
      Likely cause is that production has its own catalog
      subscription/visibility logic that does not include words
      from ielts_confusable_match.json (or similar narrow-scope
      books) for TTS playback.
  - The audio files for these 2 words are safely on OSS; they
    will become audible as soon as the production catalog
    visibility is fixed.

Tested:
  - Production metadata API verified for 128 words; 0 unexpected
    errors during synth/upload.
  - 130/130 normalize_phonetic_text round-trips unchanged.

Not-tested:
  - Human ear-judge of the 130 new MP3s (spot-check sample of 5-10
    before declaring this round 'audio-correct' at scale).

Confidence: high for the 128 successful production verifications,
medium for the 2 failed (audio is correct on OSS, production
catalog visibility is the gap).

Scope-risk: low (pure additive). The 130 overrides replace known-unsafe
markers in catalog phonetics; no schema, no business-logic, no
build-config changes.

Directive: before applying the next batch (24 disagreement fixes
+ 803 phrases), the user should:
  (a) spot-check 5-10 of the 130 new MP3s (e.g. actor, bar, bitter,
      appear, cancer) to confirm SSML phoneme hint produces
      intelligible output
  (b) investigate why 'consistency' and 'manufacturer' are 404
      on production (likely a catalog-visibility filter)
  (c) confirm the 24 disagreement recommendations do not include
      multi-POS false positives (e.g. 'abuse' has different vowels
      for different parts of speech)

## Changed Paths

- vocabulary_data/phonetic_overrides.json

## Verification

- Tested: not recorded
- Not-tested: none

## Remaining Risks

none
