# Submit Log: Vocab Test Shortcut Production Follow-up

## Scope
- `feature_wish:10` production follow-up after user reported numeric shortcuts still did not react in production.

## Changes
- Move `/vocab-test` numeric shortcut handling to a `window` capture listener so page-level key handlers cannot swallow the event before the test page sees it.
- Support physical `Digit1`-`Digit4` and `Numpad1`-`Numpad4` codes in addition to `event.key`.
- Support the fourth rendered answer option while preserving the original `1`/`2`/`3` behavior and editable-target guard.

## Verification
- `pnpm --dir frontend exec vitest run src/components/vocab-test/page/VocabTestPage.test.tsx`
- `pnpm --dir frontend run verify:repo-guards`

## Deployment
- Pending production redeploy and production keyboard smoke.
